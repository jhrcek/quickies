# Elm LLM Quickies

## How to build

### Option 1: One-shot build
Run the following command in the root of the repository:
```bash
nix run
```

### Option 2: Development shell
Enter the shell and run the script manually:
```bash
nix develop
build-all
```

